{
  "host": "${process.env.DOGGO_HOST || '0.0.0.0'}",
  "port": "${process.env.DOGGO_PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
